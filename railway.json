{
  "name": "leadgen",
  "version": 1,
  "database": {
    "plan": "free",
    "name": "leadgen_db"
  },
  "services": [
    {
      "name": "backend",
      "github": {
        "repo": "Div0011/LeadGen",
        "branch": "main"
      },
      "rootDir": "backend",
      "dockerfilePath": "docker/Dockerfile.backend",
      "buildCommand": "pip uninstall -y psycopg2-binary 2>/dev/null; pip install --no-cache-dir --force-reinstall asyncpg && pip install --no-cache-dir -r requirements.txt",
      "env": {
        "DATABASE_URL": {
          "from": {
            "service": "database",
            "attr": "DATABASE_URL"
          }
        }
      }
    }
  ]
}
